# Database Configuration - Secure Connection through PgBouncer
DATABASE_URL="postgresql://app_readwrite:secure_app_password_2024@localhost:6432/payment_template_dev?sslmode=prefer"

# Read-only database connection for analytics
DATABASE_READONLY_URL="postgresql://app_readonly:secure_readonly_password_2024@localhost:6432/payment_template_dev?sslmode=prefer"

# Database User Passwords (use strong passwords in production)
APP_DB_PASSWORD=secure_app_password_2024
APP_READONLY_PASSWORD=secure_readonly_password_2024

# PostgreSQL Administrative Passwords
POSTGRES_PASSWORD=secure_dev_password_2024
POSTGRES_REPLICA_PASSWORD=secure_replica_password_2024

# PgBouncer Administrative Passwords
PGBOUNCER_ADMIN_PASSWORD=secure_pgbouncer_admin_2024
PGBOUNCER_MONITOR_PASSWORD=secure_pgbouncer_monitor_2024

# Authentication Configuration (BetterAuth)
BETTER_AUTH_SECRET="your-super-secret-auth-key-min-32-chars"
BETTER_AUTH_URL="http://localhost:3000"
BETTER_AUTH_TRUSTED_ORIGINS="http://localhost:3000,http://localhost:3001"

# Next.js Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_STRIPE_TEST_MODE="true"

# Stripe Configuration
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Email Configuration (Optional)
RESEND_API_KEY="re_..."

# Redis Configuration (Optional)
REDIS_URL="redis://:secure_redis_password_2024@localhost:6379"
REDIS_PASSWORD=secure_redis_password_2024

# Redis Commander Web Interface
REDIS_COMMANDER_USER=admin
REDIS_COMMANDER_PASSWORD=secure_commander_password_2024

# Security Configuration
MAINTENANCE_MODE="false"